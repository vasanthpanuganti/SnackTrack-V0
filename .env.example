# ===================================
# SnackTrack Environment Variables
# ===================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# See ENV.md for full documentation on each variable.

# ---- General ----
NODE_ENV=development                # "development" | "production" | "test"
PORT=3001                           # Backend server port

# ---- Supabase (required) ----
# Get these from: Supabase Dashboard → Project Settings → API
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
SUPABASE_JWT_SECRET=your-jwt-secret

# ---- Supabase PostgreSQL / Prisma (required) ----
# Get these from: Supabase Dashboard → Project Settings → Database → Connection string
# Replace [password] with your DB password and [project-ref] with your project ref
DATABASE_URL=postgresql://postgres:[password]@db.[project-ref].supabase.co:6543/postgres?pgbouncer=true
DIRECT_URL=postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres

# ---- Redis (required) ----
# Default works with local Redis or `pnpm docker:up`
REDIS_URL=redis://localhost:6379

# ---- External APIs (required) ----
# Spoonacular: https://spoonacular.com/food-api/console
SPOONACULAR_API_KEY=your-spoonacular-api-key
# USDA FoodData Central: https://fdc.nal.usda.gov/api-key-signup
USDA_API_KEY=your-usda-api-key

# ---- ML Service ----
# Default works when ml-service runs locally or via Docker
ML_SERVICE_URL=http://localhost:8000

# ---- CORS ----
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000,http://localhost:8081

# ---- Monitoring (optional) ----
# Sentry DSN for error tracking; leave empty to disable
SENTRY_DSN=

# ---- Cloudinary (optional) ----
# Leave empty if not using. Full format: cloudinary://API_KEY:API_SECRET@CLOUD_NAME
# Get credentials from: Cloudinary Dashboard → Settings → Product environment credentials
CLOUDINARY_URL=
